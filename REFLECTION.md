# Reflection

## 1. มี​เทสต์​ทั้งหมด​กี่​ตัว​และ​โค้ด​จริง​กี่​บรรทัด​อัตราส่วน​เป็น​เท่าใด


มี Test ทั้งหมด 6 ตัว และมีโค้ดจริงประมาณ 24 บรรทัด
อัตราส่วน Test : Production Code ≈ 6 : 24 หรือ 1 : 4


## 2. ช่วง​ที่​แถบ​เป็น​สีแดง​นาน​ที่สุด​คือ​เมื่อใด​อะไร​ทำให้​มัน​นาน

ช่วง A6 ใช้เวลานานที่สุด เพราะต้องออกแบบของ `Sum` และ `Converter` แล้วก็ต้องทำตาม step


## 3. หา​จุด​ที่​ใช้ Fake It หนึ่ง​จุด​ใช้ Triangulate หนึ่ง​จุด​และ​ใช้ Obvious Implementation หนึ่ง​จุด​เมื่อ​มอง​ย้อนกลับ​ไป​แต่ละครั้ง​เลือก​ได้​เหมาะสม​หรือไม่

- Fake It: A6 ตอน `plus()` คืน `Quantity(500, "g")` ทำให้ Test ผ่านแบบง่ายๆ คิดว่าเหมาะสม
- Triangulate: A2 เพิ่ม `200 × 2 = 400` เพื่อบังคับให้ `times()` คำนวณจริงๆ แทนการ return ค่าคงที่ เหมาะสม
- Obvious Implementation: A3 ใช้ `return Quantity(self.amount * multiplier, self.unit)` เหมาะสม แก้ปัญหาได้ตรง


## 4. ยังมี​รายการ​ใด​ค้าง​อยู่​ใน​รายการ​ทดสอบ​รายการ​ใด​ที่จะ​ลงมือทำ​จริง​และ​รายการ​ใด​ที่​ควร​ลบทิ้ง​เพราะ​เป็นการ​คาดเดา​ล่วงหน้า

มีการทดสอบการบวกแบบคนละหน่วย กับการแปลงหน่วย ที่ยังไม่ได้ทำ และสิ่งที่ควรทำจริงคือ test ว่าระบบทำงานตามที่กำหนดไว้ และสิ่งที่่ควรลบคือ test ที่คิดขึ้นมาเองจากการคาดเดา

## 5. ดู​เมท็อด Quantity.reduce ถ้า​ออกแบบ​ระบบ​ทั้งหมด​บน​กระดาษ​ก่อนนักศึกษาจะ​เขียน​เมท็อด​นี้​ออกมา​หรือไม่

ถ้าออกแบบระบบทั้งหมดบนกระดาษก่อน คิดว่าไม่น่าจะเขียน `Quantity.reduce` ออกมาเพราะคิดว่าไม่จำเป็น แต่พอทำ TTD แล้วเจอว่ามันต้องใช้ reduce() เพื่อให้ sum กับ converter ทำงานด้วยกันได้ ก็คิดว่าควรจะคิดในส่วนนี้ด้วย 